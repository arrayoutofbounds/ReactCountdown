var expect = require('expect');

// describe is visual. Made to look easy. breaks tests into groups
// here the test is put in the app group
describe('App', () => {
  it('should properly run tests', () => {
    expect(1).toBe(1);
  });
});
